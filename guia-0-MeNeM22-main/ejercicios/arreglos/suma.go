package arreglos

// Escribir una función que reciba un arreglo de enteros como parámetros y devuelva la suma de todos sus elementos
// Pre: len(arreglo) > 0
// Post: Devuelve la suma de todos los elementos del arreglo.

func Suma(arreglo []int) int {
	var total int

	return total
}
